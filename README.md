# AttendX AI

AttendX AI is a simple face-recognition attendance system built using Python and OpenCV.

## Features
- Detects faces from webcam
- Matches with stored dataset
- Automatically records attendance

## Technologies
Python
OpenCV
NumPy

## How to Run
1. Install dependencies
pip install -r requirements.txt

2. Run the program
python main.py
